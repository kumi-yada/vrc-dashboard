# VRC Dashboard

## Run Application

On Linux you might need to have to set `WEBKIT_DISABLE_DMABUF_RENDERER=1` before

## Tools

- Mitmproxy - monitor http traffic, set it as [http proxy](https://wiki.archlinux.org/title/Proxy_server)